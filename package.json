{
  "scripts": {
    "lint": "yarn run lint:check",
    "lint:check": "eslint .",
    "lint:fix": "eslint --fix .",
    "prepare": "husky install"
  },
  "dependencies": {
    "@azure/msal-node": "^1.14.1",
    "@byba/eslint-config": "^1.4.0",
    "sharp": "^0.31.1"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix"
  }
}
